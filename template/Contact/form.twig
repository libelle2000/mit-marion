{% extends "pageBase.twig" %}
{% block title %}mit-marion-de{% endblock %}
{% block additionalStyleSheets %}
    <link rel="stylesheet" href="/_assets/css/contactForm.css"/>
{% endblock %}
{% block content %}
    {{ include('include/header.incl.twig') }}
    <article class="contact">
        <section class="contactIntro">
            <p>Du hättest gerne mehr Infos zu meinen Kursen? Du möchtest dich anmelden oder hast einfach nur eine Frage?</p>
            <p>Hier bist du richtig. Ich werde mich so schnell wie möglich bei dir melden.</p>
        </section>
        <form class="contactForm" action="{{ form.action }}" method="{{ form.method }}">
            <div class="formElementGroup preName">
                <label for="preName">{{ contactFormElements.preName.placeholder }}:</label>
                <input id="preName"
                       name="{{ contactFormElements.preName.identifier }}" type="text"
                       placeholder="{{ contactFormElements.preName.placeholder }}"
                       value="{{ contactFormElements.preName.customerInput }}"/>
                <ul class="errorList">
                    <li>ein error</li>
                    <li>zwei error</li>
                    <li>drei error</li>
                </ul>
            </div>
            <div class="formElementGroup surName">
                <label for="surName">{{ contactFormElements.surName.placeholder }}:</label>
                <input id="surName"
                       name="{{ contactFormElements.surName.identifier }}" type="text"
                       placeholder="{{ contactFormElements.surName.placeholder }}"
                       value="{{ contactFormElements.surName.customerInput }}"/>
                <ul class="errorList">
                    <li>ein error</li>
                </ul>
            </div>
            <div class="formElementGroup eMail">
                <label for="eMail">{{ contactFormElements.eMail.placeholder }}:</label>
                <input id="eMail"
                       name="{{ contactFormElements.eMail.identifier }}"
                       type="email" placeholder="{{ contactFormElements.eMail.placeholder }}"
                       value="{{ contactFormElements.eMail.customerInput }}"/>
                <ul class="errorList">
                    <li>ein error</li>
                </ul>
            </div>
            <div class="formElementGroup customerMessage">
                <label for="customerMessage">
                    {{ contactFormElements.customerMessage.placeholder }}:
                </label>
                <textarea id="customerMessage"
                          name="{{ contactFormElements.customerMessage.identifier }}"
                          placeholder="Hallo Marion,">{{ contactFormElements.customerMessage.customerInput }}</textarea>
                <ul class="errorList">
                    <li>ein error</li>
                </ul>
            </div>
            <div class="formElementGroup dataPrivacy">
                <label for="dataPrivacy">
                    <input id="dataPrivacy"
                           name="{{ contactFormElements.dataPrivacy.identifier }}" type="checkbox"
                           placeholder="{{ contactFormElements.dataPrivacy.placeholder }}"/>
                    {{ contactFormElements.dataPrivacy.placeholder }}
                </label>
                <ul class="errorList">
                    <li>ein error</li>
                </ul>
            </div>
            <div class="formElementGroup captcha">captcha</div>
            <input class="submit button" type="submit" value="abschicken"/>
        </form>
    </article>
    {#    <pre>#}
    {#        {{ dump() }}#}
    {#        {{ dump(contactFormElements) }}#}
    {#    </pre>#}
{% endblock %}
